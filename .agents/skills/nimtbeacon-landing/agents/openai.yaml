interface:
  display_name: "Nimtbeacon Landing"
  short_description: "Repo-specific patterns and workflows for nimtbeacon-landing"
  default_prompt: "Use the nimtbeacon-landing repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true